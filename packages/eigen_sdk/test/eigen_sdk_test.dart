import 'package:test/test.dart';

void main() {
  // Placeholder so the package resolves and CI is green before the transport
  // layers land. Replaced by REST-client and socket tests in Stage 1.
  test('package resolves', () {
    expect(true, isTrue);
  });
}
